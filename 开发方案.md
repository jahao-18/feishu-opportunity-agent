# 商机录入与分析 Agent 开发方案

## 阶段一，业务规则与数据结构设计

### 开发内容

- 整理题目要求的输出字段：
  - 客户需求
  - 核心场景
  - 预算
  - 决策人
  - 影响人
  - 时间计划
  - 商机阶段
  - 风险
  - 下一步行动
  - 未确认信息
- 将 S0–S5 阶段定义转化为机器可执行规则。
- 使用 Pydantic 定义标准输出结构。
- 为每个字段增加原文证据。
- 定义事实状态：
  - 已确认
  - 未确认
  - 存在矛盾
  - 无法判断
- 编写典型、缺失、矛盾和边界测试样例。

### 阶段产出

- `OpportunityFacts`
- `OpportunityAnalysis`
- `stage_rules.json`
- 第一批测试用例

### 验收标准

- 所有输出字段都有明确类型。
- 每个阶段都有可执行判断条件。
- 未提供的信息不能被自动补全。

## 阶段二，豆包模型接入与事实提取

### 开发内容

- 在火山方舟创建豆包模型推理接入点。
- 配置：
  - `ARK_API_KEY`
  - `ARK_BASE_URL`
  - `ARK_MODEL`
- 通过 OpenAI 兼容接口调用豆包。
- 编写事实提取 Prompt。
- 要求模型输出符合 Pydantic Schema 的 JSON。
- 增加：
  - 超时处理
  - JSON 解析
  - Schema 校验
  - 最多两次重试
  - Mock 模式兜底
- 明确限制豆包只提取事实，不直接决定商机阶段。

### 阶段产出

```text
销售拜访记录
    ↓
豆包
    ↓
带原文证据的结构化事实
```

### 验收标准

- 输出能够通过 Pydantic 校验。
- 不编造金额、姓名和日期。
- “预算尚未确认”必须输出为空或未确认。
- 模型接口失败时系统有明确提示。

## 阶段三，受控 Agent 决策系统

> 状态：已完成。已实现 Controller、多轮补充、工具注册、人工复核分支与确认后保存；客户历史和飞书任务工具将在对应集成阶段接通真实 API。

### 开发内容

- 使用 LangGraph 建立 Agent 状态。
- 增加 `controller` 决策节点。
- Agent 根据当前信息动态选择下一步，而不是固定执行全部节点。
- 注册以下 Agent 工具：
  - `extract_opportunity_facts`
  - `get_stage_rules`
  - `validate_evidence`
  - `get_customer_history`
  - `save_opportunity`
  - `create_followup_task`
- 定义 Agent 决策类型：
  - 继续分析
  - 请求补充信息
  - 请求确认矛盾
  - 保存商机
  - 建议人工复核
  - 结束分析

### 核心路径

```text
用户输入
   ↓
Controller
   ├─ 信息不足 → 追问
   ├─ 信息矛盾 → 请求确认
   ├─ 信息充分 → 分析阶段
   ├─ 分析异常 → 修复输出
   └─ 分析完成 → 等待保存确认
```

### 阶段产出

- `controller.py`
- `tools.py`
- Agent 条件分支
- 对话状态管理
- 信息补充与合并逻辑

### 验收标准

- Agent 能够根据不同输入走不同路径。
- 不再是固定的一次性工作流。
- Agent 每次只询问最关键的 1～2 个问题。
- 用户补充信息后，不丢失前面的商机状态。

## 阶段四，商机阶段规则引擎

> 状态：已完成。已实现最高阶段匹配、原文证据、下一阶段缺口解释、阶段回退检测与矛盾信号阻断。

### 开发内容

- 用确定性代码判断 S0–S5。
- 从已验证事实中读取阶段信号。
- 采用“满足的最高阶段”原则。
- 每次阶段升级必须有原文证据。
- 处理阶段回退和冲突。
- 输出：
  - 当前阶段
  - 阶段名称
  - 阶段证据
  - 未达到下一阶段的原因
  - 下一阶段所缺信息

### 判断逻辑

```text
签约或正式订单 → S5
进入立项、审批或供应商决策 → S4
讨论预算、报价、采购流程或合同条款 → S3
明确同意演示、试用或方案评估 → S2
明确业务问题或场景 → S1
只有初步接触 → S0
```

### 验收标准

- 阶段判断不由大模型自由决定。
- “预算未确认”不能触发 S3。
- S3 以上阶段必须保留升级证据。
- 同一输入多次运行结果应基本稳定。

## 阶段五，风险分析与行动规划

> 状态：已完成。已实现七类确定性风险、动态等级、事实依据、分阶段行动模板及最高风险主动追问。

### 开发内容

- 根据商机缺口生成风险。
- 为风险划分高、中、低等级。
- 根据当前阶段生成下一步行动。
- 行动内容包括：
  - 优先级
  - 具体动作
  - 建议负责人
  - 建议时间
  - 推荐原因
- 识别常见风险：
  - 预算未确认
  - 决策链不明确
  - 时间计划缺失
  - 需求价值不足
  - 客户态度模糊
  - 商务条件冲突
  - 前后信息矛盾
- 允许 Agent 针对最高风险主动追问。

### 阶段产出示例

```json
{
  "risks": [
    {
      "level": "高",
      "type": "决策链不清晰",
      "evidence": "仅提到业务负责人",
      "description": "尚未确认最终采购决策人"
    }
  ],
  "next_actions": [
    {
      "priority": "P0",
      "action": "确认最终决策人和审批路径",
      "owner": "销售负责人",
      "due_time": "下次沟通前"
    }
  ]
}
```

### 验收标准

- 每项风险都有事实依据。
- 下一步行动必须具体可执行。
- 不能只输出“建议继续沟通”等泛化内容。

## 阶段六，飞书开放平台集成

> 状态：已完成并通过真实飞书联调。企业自建应用已创建、授权、发布并加入目标多维表格；现有字段兼容、S0–S5 规则读取、客户历史、幂等新增/更新、失败重试和本地降级均已实现。联调记录重复保存后仍只有一条，证明幂等更新生效。为避免密钥进入工具输出，`.env` 中的 App Secret 仍需由项目所有者在本机手动粘贴一次。

### 开发内容

- 创建飞书企业自建应用。
- 开通多维表格读写权限。
- 发布并安装应用。
- 将应用添加为目标多维表格协作者。
- 获取并安全保存：
  - `FEISHU_APP_ID`
  - `FEISHU_APP_SECRET`
  - Base App Token
  - Table ID
- 实现 `tenant_access_token` 获取和缓存。
- 实现以下接口：
  - 查询表字段
  - 读取阶段规则
  - 查询客户历史商机
  - 新增商机记录
  - 更新商机记录
- 启动时自动检查飞书字段是否齐全。
- 飞书写入失败时，不影响网页展示分析结果。

### 飞书职责

```text
阶段规则表 → Agent 读取业务规则
商机分析表 → 保存分析结果
测试用例表 → 保存评测结果
工作流 → 内部通知和创建任务
仪表盘 → 展示销售漏斗和风险
```

### 验收标准

- 网页分析完成后，结果能够自动写入飞书。
- 每条记录包含请求编号和规则版本。
- 不在前端暴露飞书密钥。
- 飞书接口失败时可重新同步。

## 阶段七，飞书工作流与业务协同

> 状态：已完成。代码侧协同路由、飞书工作流配置、保存启用和端到端验证均已通过。

### 开发内容

- 触发条件改为“商机分析表新增记录时”。
- 不再使用受组织权限限制的单聊机器人触发。
- 根据商机结果建立条件分支：
  - S3 及以上：通知销售负责人
  - 高风险：发送人工复核提醒
  - 存在 P0 行动：创建飞书任务
  - 信息不足：标记“待补充”
  - 分析异常：进入人工复核队列
- 给飞书消息卡片添加：
  - 查看商机
  - 确认结果
  - 标记需要复核
  - 创建跟进任务
- 搭建销售漏斗和风险仪表盘。

### 实际实现

- 触发器为`商机分析表`新增记录且`请求编号`非空。
- 多分支使用“同时执行所有满足条件的分支”，允许一个商机同时通知重点阶段、高风险、P0、待补充和人工复核。
- 四类消息均绑定触发记录的`记录链接`；P0 分支创建飞书任务。
- `agent/collaboration.py`镜像飞书条件，`integrations/feishu_bitable.py`为空列表输出`无`，确保线上条件和本地测试一致。
- 具体配置见`docs/阶段七工作流配置.md`。
- 联调记录同时命中五个分支；工作流耗时 3.4 秒，四个消息节点和一个任务节点均执行成功。

### 验收标准

- 自建 Agent 写入记录后能触发飞书内部流程。
- 消息中能直接跳转到对应记录。
- 飞书工作流故障不影响外部 Agent 使用。

## 阶段八，公开体验页面开发

> 状态：本地页面开发与真实模型回归已完成；公网发布属于阶段十。

### 开发内容

- 使用 Streamlit 构建无需飞书登录的公开页面。
- 页面提供：
  - 销售拜访记录输入框
  - 三个一键测试样例
  - 开始分析按钮
  - Agent 运行状态
  - 补充信息输入
  - 用户确认按钮
  - 保存到飞书按钮
- 结构化展示：
  - 商机阶段
  - 置信度
  - 客户需求
  - 核心场景
  - 决策链
  - 预算
  - 时间计划
  - 风险
  - 下一步行动
  - 未确认信息
  - 原文证据
- 显示 Agent 运行轨迹：

```text
✓ 已提取客户需求
✓ 已加载阶段规则
✓ 已完成证据校验
✓ 当前判断为 S2
✓ 已同步至飞书
```

### 验收标准

- 面试官无需登录即可打开。
- 能独立输入并查看结果。
- 信息不足时可以继续回答 Agent 追问。
- 页面刷新或接口失败时有明确提示。

### 实际实现

- 页面采用“分析结果 / Agent 轨迹 / 补充与保存”三个视图，避免一次性堆叠全部内容。
- Agent 轨迹只展示可验证的业务步骤，不展示或伪造模型内部思维过程。
- 支持结果 JSON 下载、新建分析、输入字数提示和窄窗口响应式布局。
- 模型或飞书失败时保留上一条成功结果；飞书保存仍需用户明确确认。
- 真实运行“方案验证”样例完成豆包提取、飞书规则读取、S4 判级与 Controller 决策。
- 具体说明见`docs/阶段八公开体验页面.md`。

## 阶段九，质量门禁与自动评测

> 状态：已完成。已建立 18 条自动评测用例和飞书测试表同步；真实豆包批次 `20260912-101443-live` 为 18/18 通过，Schema 100%、阶段准确率 100%、证据保留 100%、编造率 0%、飞书同步 18/18。平均响应 15.465 秒，略高于“小于 15 秒”的理想目标，留待部署阶段继续优化。完整报告见 `evals/results/20260912-101443-live.json`，说明见 `docs/阶段九自动评测.md`。

### 开发内容

- 建立至少 15 条测试用例：
  - S0–S5 各阶段
  - 信息缺失
  - 信息矛盾
  - 模糊表达
  - 否定表达
  - 恶意提示词
  - 超长输入
- 自动检查：
  - 阶段是否正确
  - 必填字段是否完整
  - 证据是否存在于原文
  - 是否编造信息
  - 是否正确标记未确认
- 将实际结果与预期结果写入飞书测试用例表。
- 记录：
  - 模型版本
  - Prompt 版本
  - 规则版本
  - 响应时间
  - 测试是否通过

### 目标指标

- Schema 通过率：100%
- 阶段判断准确率：至少 90%
- 明确事实编造率：0
- 单次分析时间：尽量小于 15 秒
- 飞书同步成功率：至少 95%

## 阶段十，部署与面试材料

> 状态：已完成。公网直达入口为 <https://feishu-opportunity-agent.streamlit.app/~/+/>，面试官无需 GitHub、Streamlit 或飞书登录。已在匿名会话完成真实豆包分析与飞书规则读取回归；Secrets、限流、输入保护、公开写入保护、验收清单、架构图、自测样例、3 分钟演示脚本和异常备用方案均已就绪。详见 `docs/阶段十部署说明.md` 与 `docs/阶段十面试演示手册.md`。

### 开发内容

- 将项目部署到可公开访问的平台。
- 使用环境变量管理密钥。
- 增加基础限流和输入长度限制。
- 使用手机、无痕浏览器和外部网络测试。
- 准备：
  - 公开访问链接
  - 使用说明
  - 架构图
  - Agent 规则说明
  - 输出字段说明
  - 至少一个自测输入输出
  - 3 分钟演示脚本
  - 异常情况备用方案
- 保留 Mock 模式作为现场 API 故障兜底，但正式链接默认使用豆包。

### 最终交付

```text
1. 公开 Agent 链接
2. 飞书商机分析后台
3. 飞书阶段规则配置表
4. 飞书工作流和仪表盘
5. 项目源代码
6. README 和部署说明
7. 测试报告
8. 自测样例
```

## 最终产品定位

> 一个基于 LangGraph 和豆包构建的受控商机分析 Agent。Agent 能够根据当前商机信息动态提取事实、主动追问、调用业务规则、识别风险并规划下一步；分析结果通过飞书开放平台沉淀到多维表格，再由飞书工作流和仪表盘承接企业内部协作。
